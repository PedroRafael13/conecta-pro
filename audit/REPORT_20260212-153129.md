# AUDITORIA CONECTA PRO - RELATORIO EXECUTIVO

- Data: $(date -Iseconds)
- Escopo: backend + frontend + seguranca + qualidade + testes
- Metodo: auditoria do zero com execucao de scans e suites locais

## Veredito

**NAO PRONTO PARA PRODUCAO**.

Bloqueadores:
- P0: segredos/chaves privadas versionados no Git.
- P1: vulnerabilidades conhecidas em dependencias de runtime.
- P1: gates de qualidade quebrados (lint/typecheck).
- P1: fluxo OAuth com tokens em query string.

## Evidencias Principais

### 1) Segredos e credenciais no repositorio (P0)
Arquivos sensiveis versionados detectados (amostra):
- `credentials/.env.credentials`
- `credentials/certificates/a1_key.pem`
- `credentials/certificates/inter_api.key`
- `credentials/certificates/inter_key.pem`
- `credentials/certificates/certificado.pfx`
- `certs/conecta_certificado.p12`
- `frontend/.env.production`

Chaves privadas encontradas em texto:
- `credentials/certificates/inter_key.pem:4`
- `credentials/certificates/inter_api.key:1`
- `credentials/certificates/a1_key.pem:4`

### 2) Dependencias vulneraveis (P1)
Python (`pip-audit -r requirements.txt`):
- 16 vulnerabilidades em 7 pacotes.
- Destaques:
  - `sentry-sdk==1.40.0` (CVE-2024-40647)
  - `h11==0.14.0` (CVE-2025-43859)
  - `python-multipart==0.0.20` (CVE-2026-24486)
  - `starlette==0.41.3` (2 CVEs)
  - `aiohttp==3.11.11` (multiplas CVEs)
  - `pillow==11.0.0` (CVE-2026-25990)

Node (`npm audit --omit=dev` no frontend):
- 2 vulnerabilidades High:
  - `axios` (DoS via mergeConfig)
  - `xlsx` (Prototype Pollution/ReDoS)

### 3) OAuth/token em URL (P1)
No callback OAuth, o frontend le token na query string:
- `frontend/src/app/auth/callback/page.tsx`
  - `searchParams.get("access_token")`
  - `searchParams.get("refresh_token")`

Tambem ha uso de token em query para websocket:
- `frontend/src/hooks/notifications/useNotificationWebSocket.ts`
- `frontend/src/lib/websocket.ts`

### 4) Qualidade e gates (P1)
Backend lint (`ruff check modules core api`):
- Falha com 1 erro:
  - `modules/ai/report_generator/models/report_section.py:203` (A003)

Frontend lint (`npm run lint`):
- Falha com 30 erros e 6 warnings.
- Erros em docs/e2e/tests, incluindo `react/jsx-no-undef`, `react-hooks/rules-of-hooks`, `@next/next/no-assign-module-variable`.

Frontend typecheck (`npm run type-check`):
- Falha com diversos erros TS, majoritariamente em suites de teste.

Frontend build (`npm run build`):
- Build concluido com sucesso.
- Warning de config invalida:
  - `experimental.serverComponentsExternalPackages` (migrar para `serverExternalPackages`).

### 5) Testes
Backend (`./venv/bin/pytest -q`):
- `6232 passed, 43 skipped, 12 xfailed, 3404 warnings`.

Frontend (`npm run test:run`):
- `125 files passed, 2468 tests passed`.

E2E (`npm run test:e2e -- --list`):
- `2985 tests em 138 arquivos`.
- Nao executados nesta auditoria (somente listagem).

## Conclusao

Mesmo com boa saude de testes unitarios/integracao, o projeto permanece **NAO PRONTO** por risco de seguranca imediato (P0) e gates obrigatorios quebrados (P1).
