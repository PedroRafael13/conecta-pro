# WORK_PLAN (20260211-234159)

## Objetivo
Encerrar P0/P1 e liberar o sistema para producao com evidencias repetiveis.

## Prioridades (ordem sugerida)

### P0 — Segredos e chaves
1) Remover segredos do repo
   - Alvos: `.env`, `docker-compose*.yml`, `credentials/certificates/*`, scripts com tokens
   - Acao: migrar para secret manager (Vault/SM), `.env.example` somente com placeholders
2) Rotacionar chaves
   - JWT, Redis, DB, API tokens, certificados, OAuth/LLM
   - Validar revogacao e novos secrets no ambiente

### P1 — Dependencias vulneraveis
3) Python: atualizar/pinar
   - Atualizar `sentry-sdk` e `h11` e pinar `cryptography`, `hvac`, `PyJWT`
   - Rodar `safety scan` (novo comando) e validar sem vulnerabilidades
4) Node: mitigar `axios` e `xlsx`
   - Atualizar axios para versao corrigida
   - Avaliar alternativa/mitigacao para xlsx (ou isolar uso)
   - Rodar `npm audit --omit=dev`

### P1 — Build/Lint/Typecheck
5) Corrigir `next.config.ts`
   - Migrar `experimental.serverComponentsExternalPackages` -> `serverExternalPackages`
   - Reexecutar `npm run build`
6) Limpar lint/typecheck
   - Backend: `ruff` e `mypy` (modulo duplicado services)
   - Frontend: `eslint` (docs/coverage), `tsc` (tests/mocks)

### P1 — OAuth seguro
7) Remover tokens do query string
   - Trocar por cookie httpOnly ou code exchange com backend
   - Atualizar fluxo de callback frontend

### P2 — Qualidade e DX
8) Resolver warnings de `datetime.utcnow()`
9) Ajustes de acessibilidade (Dialog/AlertDialog)
10) Revisar `eval` em report_section e substituir por parser seguro

## Validacao final
- `ruff check` + `mypy`
- `npm run lint` + `npm run type-check`
- `npm run build`
- `pytest -q`
- `vitest run`
- Executar subset E2E por modulo critico

## Evidencias
- Atualizar `REPORT_20260211-234159.md` e `CHECKLIST_PROD_20260211-234159.md`
- Atualizar `E2E_MATRIX_20260211-234159.md` com status real
