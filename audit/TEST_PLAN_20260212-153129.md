# TEST PLAN - AUDITORIA ATUAL

## Executado
- Backend: `./venv/bin/pytest -q`
- Frontend: `npm run test:run`
- Frontend build: `npm run build`
- Frontend lint: `npm run lint`
- Frontend typecheck: `npm run type-check`
- Backend lint: `ruff check modules core api`

## Seguranca executada
- `./venv/bin/pip-audit -r requirements.txt`
- `./venv/bin/safety check -r requirements.txt`
- `./venv/bin/bandit -r modules core api -q`
- `npm audit --omit=dev --json`
- Busca de segredos/keys no git e working tree

## Proximo ciclo (obrigatorio antes de deploy)
1. Reexecutar lint/typecheck apos correcoes.
2. Rodar E2E por dominio critico (auth, financeiro, operacional, seguranca LGPD).
3. Validar fluxo OAuth revisado sem token em URL.
4. Reexecutar scans de seguranca e registrar delta de CVEs.
